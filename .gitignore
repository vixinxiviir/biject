# Rust build artifacts
/target/
tauri-app/src-tauri/target/

# Generated run outputs
/outputs-*/
/myresults

# Local scratch fixtures (real fixtures live in examples/)
/source.csv
/target.csv

# Internal planning notes — not for the public repo or the published crate
*-roadmap.md

# Tauri mobile icon outputs
tauri-app/src-tauri/icons/android/
tauri-app/src-tauri/icons/ios/

# Local virtual environments
/.venv/

# Local tooling/editor folders
/.vscode/
/.continue/

# OS/editor noise
.DS_Store
Thumbs.db
*.swp
*.swo
desktop.ini

# Env/config secrets
.env
.env.*
!.env.example

# Cargo lock for libraries (keep for binaries/apps — this repo has both)
# Cargo.lock is intentionally tracked

# Tauri WiX/NSIS installer intermediates
tauri-app/src-tauri/WixTools/
tauri-app/src-tauri/*.msi
tauri-app/src-tauri/*.exe

# Node (if npm is ever added)
node_modules/
tauri-app/node_modules/
.aider*
